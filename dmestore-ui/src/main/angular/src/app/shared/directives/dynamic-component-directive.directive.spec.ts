import { DynamicComponentDirectiveDirective } from './dynamic-component-directive.directive';

describe('DynamicComponentDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicComponentDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
